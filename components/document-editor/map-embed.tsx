'use client'

/**
 * Composant MapEmbed - Intégration de cartes dans documents
 * 
 * NOTE: Composant non implémenté - Fonctionnalité prévue pour une future version
 * Ce composant permettra d'insérer des cartes interactives (Google Maps, OpenStreetMap) dans les documents.
 */

interface MapEmbedProps {
  onInsert?: (html: string) => void
  onClose?: () => void
}

export function MapEmbed(props?: MapEmbedProps) {
  return null
}
